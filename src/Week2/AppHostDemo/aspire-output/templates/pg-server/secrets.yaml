---
apiVersion: "v1"
kind: "Secret"
metadata:
  name: "pg-server-secrets"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "pg-server"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
stringData:
  POSTGRES_PASSWORD: "{{ .Values.secrets.pg_server.pg_server_password }}"
type: "Opaque"
