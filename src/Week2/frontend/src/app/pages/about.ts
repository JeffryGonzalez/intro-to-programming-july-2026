import { Component } from '@angular/core';

@Component({
  selector: 'app-about',
  imports: [],
  providers: [],
  template: ` <p>about works!</p> `,
  styles: ``,
})
export class About {
  // svc = inject(ShowsData);
}
